# Category Rush

Multiplayer Category Rush game.

## Run locally

```bash
npm install
npm start
```

Open http://localhost:3000

## Render

Build command: `npm install --package-lock=false`
Start command: `node server.js`
